export default defineAppConfig({
  ui: {
    drawer: {
      slots: {
        content: 'w-full max-w-[750px] mx-auto'
      }
    },
    colors: {
      primary: 'teal',
      neutral: 'stone'
    }
  }
})
